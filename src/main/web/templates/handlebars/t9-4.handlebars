{{> templates/handlebars/partials/header}}
<main id="main" role="main" tabindex="-1">
	<div class="wrapper panel--top-mar">
		{{> templates/handlebars/partials/breadcrumb}}
		<p class="page-header__title">
			Statistical bulletins
		</p>
		<h1 class="page-header__title flush--half--bottom">{{title}} statistical bulletins</h1>
		<p>
			{{#if contentSearchResult.results}}
			<span class="stand-out-text">{{contentSearchResult.numberOfResults}}</span> results for <strong>statistical bulletins</strong> released to <strong>{{title}}</strong>. You have chosen to expand these results to <strong>include all releases over time</strong><span class="icon icon-cancel" role="presentation"></span>
			{{else}}
			Sorry, there are no <strong>statistical bulletins</strong> for <strong>{{title}}</strong> articles.
			{{/if}}
		</p>
	</div>
	<!-- /wrapper -->
	<div class="wrapper panel--mar">
		{{#if contentSearchResult.results}}
		<div class="grid-wrap panel--bottom-mar">
	    	<div class="grid-col desktop-grid-one-quarter desktop-grid-push-three-quarters tablet-grid-full-width">
	        	<label for="select">Sort by
	        		<div class="field">
						<select class="input " id="select" name="">
							<option value="1" selected>Relevance</option>
							<option value="2">Release date</option>
							<option value="3">Title</option>
						</select>
	            	</div>
	        	</label>
	        </div>
	    </div>
	    <div class="grid-wrap">
		    <div class="grid-col desktop-grid-one-third">
		        <article class="box box--padded sectioned">
		        	<form id="filters">
		            	<div class="panel panel--bottom-pad panel--separated--bottom flush">
			            	<h3 class="flush float-left">Refine results</h3>
			                <button type="reset" value="Reset" class="btn btn--primary btn--small btn--thin btn--narrow btn--clear-results float-right">Clear</button>
		            	</div>
						<div class="box__content">
							<p>
			            		You can also expand these results to include the following:
			                </p>
			                <input type="checkbox" name="checkbox" value="1" checked>
			                <label for="checkbox">
			                    Show all releases over time
			                </label>
		    			</div>
		        	</form>
		        </article>
		        <!-- /box 'refine results -->
				<article class="box box--purple box--purple--separated--left">
					<div class="box__inner border box--padded">
						<header class="box__header">
							<h3 class="gamma">Need an earlier release?</h3>
						</header>
						<div class="box__content">
							<p>
								We moved them to <a href="#">The National Archives</a> website, to keep this website as responsive as possible.
							</p>
							<p>
								<strong>Please note: </strong>all historical data is still on this website.
							</p>
						</div>
					</div>
					<!-- /box__inner -->
				</article>
				<!-- /box -->
			</div>
			<!-- /desktop-grid-one-third -->
			<div class="grid-col desktop-grid-two-thirds">
				<ul class="list--neutral">
					{{#each contentSearchResult.results}}
					<li>
						<h3 class="flush"><a href="{{url}}">{{title}}</a></h3>
						<p class="flush description">
							Released: {{df releaseDate}}
						</p>
						{{md summary}}
					</li>
					{{/each}}
				</ul>
			</div>
			<!-- /desktop-grid-two-thirds -->
			{{/if}}
		</div>
		<!-- /grid-wrap -->
	</div>
	<!-- /wrapper panel--mar -->
</main>
{{> templates/handlebars/partials/footer}}

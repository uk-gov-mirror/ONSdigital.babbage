{{#partial "chart-config"}}
options.chart.type="scatter";
options.xAxis.categories=[]; {{!-- override categories --}}
options.series=[{{#each headers}}{{#if @even}}{
	name:"{{this}}",
	data: [{{#each data}}[{{alt (get this ..) "null"}},{{alt (get this (get headers ../@index_1)) "null"}}],{{/each}}],
},
{{/if}}{{/each}}];

options.plotOptions.scatter= {
        marker: {
            radius: 5,
            states: {
                hover: {
                    enabled: true
                }
            }
        },
        states: {
            hover: {
                marker: {
                    enabled: false
                }
            }
        },
        tooltip: {
            headerFormat: '<b>{series.name}</b><br>',
            pointFormat: '{point.x}, {point.y}'
        }
};

{{/partial}}

{{!-- This denotes the template inherits from base-chart-config--}}
{{> highcharts/config/base-chart-config}} 
{{> templates/handlebars/partials/header}}
<main id="main" role="main" tabindex="-1">
	<div class="page-header slate slate--blue">
		<div class="wrapper">
			{{> templates/handlebars/partials/breadcrumb}}
			<p class="page-header__title">
				<strong>Compendium</strong>
			</p>
			<h1 class="page-header__title flush--half--bottom">{{name}}</h1>
			<div class="grid-wrap">
				<p class="grid-col tablet-grid-one-quarter page-header__meta-block-with-img">
					{{#if description.nationalStatistic}}
					<img src="../../node_modules/ONS-Pattern-Library/ui/img/national-statistics.png" alt="National Statistics logo">
					{{/if}}
					<span>Contact: </span></br>
					<a href="mailto:{{description.contact.email}}">{{#if description.contact.name}}{{description.contact.name}}{{else}}{{description.contact.email}}{{/if}}</a>
				<p class="grid-col tablet-grid-one-quarter divider--border-left">
					<span>Release date: </span></br>{{df description.releaseDate}}
					</br>
					<a href="#">previous versions</a>
				</p>
				<p class="grid-col tablet-grid-one-quarter divider--border-left">
					<span>Next release: </span></br>{{df description.nextRelease}}
				</p>
				<p class="grid-col tablet-grid-one-quarter divider--border-left">
					<a href="#" class ="icon-print">&nbsp;Print full report</a>
					</br>
					<a href="#" class="icon-download">&nbsp;Download PDF</a> (38KB)
				</p>
			</div>
			<!-- /grip-wrap -->
			{{> templates/handlebars/partials/social-icons}}
		</div>
		<!-- /wrapper -->
	</div>
	<!-- /page-header -->

	<div class="wrapper panel--mar">
		<div class="grid-wrap">
			<div class="grid-col desktop-grid-full-width">
				<h2 class="flush">
					&lt; Return to <a href="{{parent.uri}}">{{parent.title}}</a>
				</h2>
			</div>
			<!-- /desktop-grid-full-width -->
		</div>
		<!-- /grid-wrap -->
	</div>
	<!-- /wrapper -->
	<div class="wrapper panel--mar panel--bottom-pad panel--separated--bottom">
		<ol class="table-of-contents-ordered-list">
			{{#each sections}}
			<li>
				<a href="#{{uri}}">{{title}}</a>
			</li>
			{{/each}}
		</ol>
		<!-- /table-of-contents -->
	</div>
	<!-- /wrapper panel--separated--bottom -->
	<div class="wrapper panel--mar ">
		<div class="grid-wrap">
			<div class="grid-col desktop-grid-two-thirds">
				{{#each sections}}
				<h2 id="{{uri}}">{{title}}</h2>
				<p>
					{{md markdown}}
				</p>
				{{/each}}
				{{#if accordions}}
					{{#each accordions}}
					<div class="accordion is-collapsed">
						<h3 id="sc-{{@index}}" class="accordion__title" aria-controls="content-{{@index}}">{{title}}</h3>
						<div id="content-{{@index}}" class="accordion__content" aria-labelledby="sc-{{@index}}">
							<p>
								{{md markdown}}
							</p>
						</div>
					</div>
					<!-- /accordion -->
					{{/each}}
				{{/if}}
			</div>
			<!-- /desktop-grid-two-thirds -->
			<div class="grid-col desktop-grid-one-third">
				<article class="box solid box--green--separated--left">
					<div class="box__inner border box--padded">
						<h3 class="box__section__header">Data in this chapter</h3>
						<div class="box__content">
							<ul class="list--neutral">
								{{#each data}}
								<li class="flush--third--bottom">
									<a href="{{uri}}">{{title}}</a>
								</li>
								{{/each}}
							</ul>
						</div>
					</div>
					<!-- /box__inner -->
				</article>
				<!-- /box -->
				<div class="box box--padded sectioned">
					<h3 class="box__section__header">Contact details for this compendium</h3>
					<div class="box__content">
						<address>
							{{#if description.contact.name}}{{description.contact.name}}{{/if}}
		                    <a href="mailto:{{description.contact.email}}">{{description.contact.email}}</a></br>
		                    {{#if description.contact.telephone}}Telephone: {{description.contact.telephone}}{{/if}}
						</address>
					</div>
				</div>
				<!-- /box -->
			</div>
			<!-- /desktop-grid-one-third -->
		</div>
		<!-- /grid-wrap -->
	</div>
	<!-- /wrapper -->
	<div class="wrapper panel--top-pad slate--mid-grey">
		<div class="grid-wrap">
			<div class="grid-col desktop-grid-two-thirds">
				<article class="box box--teal--separated--left">
					<div class="box__inner border box--padded">
						<h3>Related articles and bulletins</h3>
						<div class="box__content">
							<ul class="list--neutral">
								{{#each relatedBulletins}}
								<li>
									<h4 class="flush"><a href="{{uri}}">{{title}}</a></h4>
									{{md description}}
								</li>
								{{/each}}
							</ul>
						</div>
					</div>
					<!-- /box__inner -->
				</article>
				<!-- /box -->
			</div>
			<!-- /grid-desktop-two-thirds -->
			<div class="grid-col desktop-grid-one-third">
				<article class="box box--orange box--orange--separated-left">
                	<div class="box__inner border box--padded ">
		                <h3>Looking for local statistics?</h3>
		                <p><a href="/local">A handy guide to let you know where to find local statistics.</a></p>
                	</div>
           		</article>
			</div>
			<!-- /desktop-grid-one-third -->
		</div>
		<!-- /grid-wrap -->
	</div>
	<!-- /wrapper -->
</main>
{{> templates/handlebars/partials/footer}}

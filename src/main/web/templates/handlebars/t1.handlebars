{{> partials/header}}

<div class="page-header slate">
    <div class="wrapper">
        <h1 class="page-header__title flush--half--bottom">Welcome to the Office for National Statistics</h1>

        <p>{{description.summary}}
  </div>
    <!-- / wrapper -->
</div>
<!-- panel -->
<div class="wrapper panel--mar">
    <!-- grid-wrap -->
    <h2>Statistical highlights</h2>

    <div class="grid-wrap grid-wrap--home-hero panel--separated--bottom">
      <!-- grid-col -->
        {{#each sections}}
        <div class="grid-col grid-col--home-hero tablet-wide-grid-one-fifth">
            <article class="nav-panel nav-panel--stats nav-panel--home-hero sectioned">
                <div class="js-home-hero-clickable-section">
                    <header class="nav-panel__header">
                        <h2 class="delta flush">{{statistics.data.description.title}}</h2>
                    </header>
                    <div class="stat__figure--home">
                        {{statistics.data.description.preUnit}} {{statistics.data.description.number}} {{statistics.data.description.unit}}
                    </div>
                    <div class="stat__description">
                        {{statistics.data.description.date}}
                    </div>
                    <img class="stat__image--home" src="{{statistics.data.uri}}/sparkline" alt="{{statistics.data.description.title}}">
                    
                    <a class="js-hidden" href="{{statistics.data.uri}}">More info</a>
                </div>
                <footer class="nav-panel__footer-home-hero">
                    <a href="{{theme.uri}}">Explore {{theme.description.title}}</a>
                </footer>
            </article>
            <!-- grid-col -->
        </div>
        {{/each}}
        <!-- /grid-wrap -->
        </div>
        <!-- /panel-->
    </div>
</div>

<div class="panel wrapper panel--separated--bottom">
    <h2 class="delta flush">New this week</h2>
    <div class="grid-wrap">
        <ul class="list--neutral">
            {{#each releases}}
				<li class="grid-col desktop-grid-one-third">
					<h3><a href="{{link}}" title="{{name}}">{{name}}</a></h3>
					<p>
                        {{description}}
					</p>
				</li>
            {{/each}}
        </ul>
        <!-- /grid-wrap -->
    </div>
    <a href="#">View new and upcoming releases</a>
    <!-- /section -->
</div>
<!-- wrapper -->
<div class="wrapper panel--mar">
    <div class="grid-wrap">
		{{#each highlightedContent}}
        <div class="grid-col box--padded-vertical nav--block-home-highlighted-content">
			<h2 class="delta flush--half">{{name}}</h3>
            <article class="box flush--half--bottom">
                <div class="box__inner border box--padded">
                    <div class="box__section">
                        <h3 class="box__section__header">
                            Image here
                        </h3>
                    </header>
                    <!-- /box__header -->
                    <div class="box__content">
                            <a href="#">Link</a>
                    </div>
                    <!-- /box__content -->
                </div>
                <!-- /box__inner -->
            </article>

            <p class="flush--half">{{description}}</p>
        </div>
		{{/each}}
	</div>
    <!-- /grid-wrap -->
</div>

{{> partials/footer}}
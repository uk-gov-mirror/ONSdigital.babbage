<li class="flush">
    <label for="checkbox-{{id}}">
        <input id="checkbox-{{id}}" class="js-auto-submit__input" type="checkbox" name="filter" value="{{value}}" {{#contains parameters.filter value}}checked{{/contains}}>
        {{label}} ({{nf docCount alt=0}})
        {{#if explanation}}
        <br>
        ({{explanation}})
        {{/if}}
    </label>
</li>
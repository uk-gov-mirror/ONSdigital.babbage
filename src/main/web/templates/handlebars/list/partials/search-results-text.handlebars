<p>
	{{#if results}}
	<span class="stand-out-text">
	{{numberOfResults}}</span> results for
	{{else}}
	Sorry, no results were found for
	{{/if}}
	<strong>‘{{parameters.q.0}}’</strong>
	{{#if parameters.type}}
	, showing <strong>{{#each parameters.type}}{{> type-label type=this plural=true}}<span class="icon icon-cancel js-enhance--show" role="presentation"></span>{{#unless @last}},{{/unless}}{{/each}}</strong>.
	{{/if}}
	{{#if parameters.includeStatics}}
	You have chosen to expand these results to include <strong>all other website content</strong><span class="icon icon-cancel js-enhance--show" role="presentation"></span>
	{{/if}}
	{{#unless results}}
	<div class="col col--lg-42">
		<ul>
			<li>{{labels.search-diff-words}}</li>
			<li>{{labels.search-remove-filters}}</li>
			{{#unless parameters.includeStatics}}
			<li><a href="/search?q={{parameters.q.0}}{{#each parameters.type}}&type={{this}}{{/each}}{{#if parameters.methodology.0}}&methodology=1{{/if}}&includeStatics=1">{{labels.search-expand-all}}</a></li>
			{{/unless}}
		</ul>
	</div>
	{{/unless}}
</p>
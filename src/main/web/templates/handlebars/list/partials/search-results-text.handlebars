<p>
	{{#if_any results featuredResult.results}}
	<span class="stand-out-text">
	{{numberOfResults}}</span> results for
	{{else}}
	Sorry, no results were found for
	{{/if_any}}
	<strong>‘{{parameters.q.0}}’</strong>
	{{#if parameters.filter}}
	showing <strong>{{#each parameters.filter}}{{> filter-label this}}<span class="icon icon-cancel js-enhance--show" role="presentation"></span>{{#unless @last}},{{/unless}}{{/each}}</strong>
	{{/if}}
	.
	{{#if parameters.includeStatics}}
	You have chosen to expand these results to include <strong>all other website content</strong><span class="icon icon-cancel js-enhance--show" role="presentation"></span>
	{{/if}}
	{{#unless results}}
	{{#unless featuredResult.results}}
	<div class="col col--lg-42">
		{{labels.search-try-other}}
		<ul>
			<li>{{labels.search-diff-words}}</li>
			<li>{{labels.search-remove-filters}}</li>
			{{#unless parameters.includeStatics}}
			<li><a href="/search?q={{parameters.q.0}}{{#each parameters.filter}}&filter={{this}}{{/each}}&includeStatics=1">{{labels.search-expand-all}}</a></li>
			{{/unless}}
		</ul>
	</div>
	{{/unless}}
	{{/unless}}
</p>
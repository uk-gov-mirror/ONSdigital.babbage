<p>
	{{#if results}}
	<span class="stand-out-text">
	{{numberOfResults}}</span> results
	{{else}}
	Sorry, there were no results
	{{/if}}
	{{#if parameters.type}}
	for <strong>{{#each parameters.type}}{{> type-label type=this plural=true}}<span class="icon icon-cancel js-enhance--show" role="presentation"></span>{{#unless @last}},{{/unless}}{{/each}}</strong>
	{{/if}}
	related to
	<strong>{{#each breadcrumb}}{{#if @last}}{{description.title}}{{/if}}{{/each}}</strong>.
	{{#if parameters.allReleases}}
	You have chosen to expand these results to <strong>include all releases over time</strong><span class="icon icon-cancel js-enhance--show" role="presentation"></span>
	{{/if}}
	{{#unless results}}
	<div class="col col--lg-42">
		<ul>
			<li>{{labels.search-diff-words}}</li>
			<li>{{labels.search-remove-filters}}</li>
		</ul>
	</div>
	{{/unless}}
</p>
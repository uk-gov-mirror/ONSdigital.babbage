{{!-- Need separate resolve for list page title location in taxonomy because breadcrumb isn't available from main.handlebars --}}
{{#resolve (parentPath uri) filter="description"}}
    {{#if upper}}
        {{description.title}}{{#if description.edition}}: {{description.edition}}{{/if}}
    {{else}}
        {{lower description.title}}
    {{/if}}
{{/resolve}}
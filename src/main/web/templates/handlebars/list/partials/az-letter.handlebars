<li class="filters__a-z-list-item"><input name="az" type="radio" id="radio__{{letter}}" value="{{letter}}" {{#if (eq (lowercase parameters.az.0) letter)}}checked{{/if}}{{#unless (get result.docCounts letter)}} disabled{{/unless}}><label for="radio__{{letter}}">{{capitalize letter}}</label>
</li>

{{#if versions}}
	<section class="show-hide__content alert--warning__content section__content--markdown">
		{{#if_all versions alerts}}
			<h3 class="alert--correction__title">Corrections</h3>
		{{/if_all}}
		{{#each versions}}
			<p class="alert__date">{{df updateDate}}</p>
			{{md correctionNotice}}
			<a class="btn btn--primary margin-bottom-sm--3 margin-bottom-md--3 print--hide" href="{{uri}}">View superseded version</a>
		{{/each}}
	</section>
{{/if}}

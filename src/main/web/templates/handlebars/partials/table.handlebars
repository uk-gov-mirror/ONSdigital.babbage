{{!--Not having line breaks before and after the div markups breaks tables displaying on the page--}}

<div class="markdown-table-container print--avoid-break">
    <h4>{{title}}</h4>

    <div class="markdown-table-wrap">
        {{{html}}}
    </div>

    <h6>Download this table</h6>

    <form method="get" action="/generator" class="inline">
        <button type="submit" title="Download CSV" class="btn btn--primary btn--thick download-analytics">
            .csv
        </button>
        <input type="hidden" name="format" value="csv"/>
        <input type="hidden" name="uri" value="{{absolute uri}}"/>
    </form>

    <form method="get" action="/generator" class="inline">
        <button type="submit" title="Download XLS" class="btn btn--primary btn--thick download-analytics">
            .xls
        </button>
        <input type="hidden" name="format" value="xls"/>
        <input type="hidden" name="uri" value="{{absolute uri}}"/>
    </form>

</div>

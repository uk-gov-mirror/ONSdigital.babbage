{{#if relatedData}}
	<div class="tiles__item tiles__item--nav-type flush-col print--hide">
		<h3 class="tiles__title-h3 tiles__title-h3--nav">Data in this {{> type-label type_code=type }}</h3>
		<div class="tiles__content--nav">
			<ul class="list--neutral">
				{{#each relatedData}}
					{{#resolve this.uri filter="title"}}
						<li><a href="{{absolute uri}}">{{title}}</a></li>
					{{/resolve}}
				{{/each}}
			</ul>
		</div>

		{{!-- Shows link to view all spreadsheets at that topic level --}}
		{{!-- <div class="tiles__extra margin-bottom-md--2">-->
			<!--<a href="{{parentPath (parentPath uri)}}/datalist">View all data related to {{breadcrumb.2.description.title}}</a>-->
		<!--</div> --}}
	</div>
{{/if}}
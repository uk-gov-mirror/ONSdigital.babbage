{{#partial "block-content"}}
    <div class="wrapper">
        <article class="col col--md-36 col--lg-39 page-content__main-content padding-right-md--1">
            {{#if markdown.0}}
                <div class="section__content--static-markdown">
                    <section>
                        <h2>About this article</h2>
                        {{md markdown.0}}
                    </section>
                </div>
            {{/if}}

            {{!-- TODO Remove this {{if}}, any publication downloads should be storing uri for PDF in the same node --}}
            {{#if_eq type "static_methodology_download"}}
                {{#if pdfDownloads}}
                    <h2>Download this article</h2>
                    <ul class="list--neutral margin-top--negative-one-fix">
                        {{#each pdfDownloads}}
                            <li>
                                <a href="/file?uri={{lowercase (concat uri "/" this.file)}}">
                                    {{title}} ({{fe (lowercase (concat uri "/" this.file))}}, {{fs (lowercase (concat uri "/" this.file))}})
                                </a>
                                <input type="hidden" name="uri" value="{{lowercase (concat uri "/" this.file)}}"/>
                            </li>
                        {{/each}}
                    </ul>
                {{/if}}
            {{else}}
            	{{#if downloads}}
                    <h2>Download this article</h2>
                    <ul class="list--neutral margin-top--negative-one-fix">
                        {{#each downloads}}
                            <li>
                                <a href="/file?uri={{lowercase (concat uri "/" this.file)}}">
                                    {{title}} ({{fe (lowercase (concat uri "/" this.file))}}, {{fs (lowercase (concat uri "/" this.file))}})
                                </a>
                            </li>
                        {{/each}}
                    </ul>
                {{/if}}
            {{/if_eq}}

        </article>

        <div class="col col--md-11 col--lg-20 margin-top-md--3">
            {{!-- {{#if downloads}}
                 <div class="tiles__item tiles__item--nav-type flush-col">
                     <h3 class="tiles__title-h3 tiles__title-h3--nav">
                         Related downloads
                     </h3>
                     <div class="tiles__content--nav">
                         <ul class="list--neutral">
                             {{#each downloads}}
                                 <li>
                                     <a href="/file?uri={{concat uri "/" (fn file)}}">{{title}}</a>
                                     ({{fs (concat uri "/" (fn file))}} {{fe (concat uri "/" (fn file))}})
                                 </li>
                             {{/each}}
                         </ul>
                     </div>
                 </div>
             {{/if}} --}}
            {{#block "publication-sidebar"}}
            {{/block}}

        </div>

    </div> {{!-- /wrapper --}}
{{/partial}}
{{!-- Inheriting from statistics template--}}
{{> content/base/statistics}}
<div class="page-header slate">
	<div class="wrapper">
		{{> partials/breadcrumb}}
		<h1 class="page-header__title flush--half--bottom">{{description.title}}</h1>
		<div class="content-reveal lede">
			<p>
				{{description.summary}}
			</p>
		</div>
	</div>
	<!-- /wrapper -->
</div>
<!-- /page-header -->
<div class="wrapper">
	<div class="grid-wrap">
		<ul class="list--neutral flush">
			{{#each sections}}
				{{#ifany data.items data.datasets data.statsBulletins data.relatedArticles}}
					{{> partials/t2/t2-section}}
				{{else}}
					{{#eq data.type 'taxonomy_landing_page'}}
						{{#if data.sections}}
							{{> partials/t2/t2-section}}
						{{/if}}
					{{/eq}}
				{{/ifany}}
			{{/each}}
		</ul>
		<div class="grid-col desktop-grid-one-third tablet-grid-one-half clear-left box--padded-vertical">
			{{> partials/local-box}}
		</div>
		<!-- /grid-col -->
	</div>
	<!-- /grid-wrap -->
</div>
<!-- /wrapper -->